public class Solution31 {
    public static double findMedianSortedArrays(int[] nums1, int[] nums2) {
        int astart = 0;
        int bstart = 0;


        if ((nums1.length + nums2.length) % 2 == 0){
            int order1,order2;
            order1=(nums1.length + nums2.length)/2 -1;
            order2 = (nums1.length + nums2.length)/2;

        }

        return 0;
    }
}
